{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --workers=2 --threads=4 --worker-class=gthread bot:app",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
